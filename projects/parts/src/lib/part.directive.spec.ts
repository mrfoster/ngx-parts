import { PartDirective } from './part.directive';

describe('PartDirective', () => {
  it('should create an instance', () => {
    const directive = new PartDirective();
    expect(directive).toBeTruthy();
  });
});
